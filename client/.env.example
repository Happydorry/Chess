# Origin of the deployed chess server (Socket.IO + Express).
# Set this in your host's build/environment settings for production, e.g.:
#   VITE_SERVER_URL=https://your-chess-server.onrender.com
# If unset, the client falls back to http://localhost:5001 for local dev.
VITE_SERVER_URL=
